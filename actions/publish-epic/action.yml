name: 'Godot Publish to Epic Games Store'
description: 'Publishes exported build to Epic (stub)'
inputs:

  epic_credentials:
    description: 'Epic credentials or token (TBD)'
    required: true

  build_path:
    description: 'Path to exported build'
    default: "builds/web/epic"

runs:
  using: 'composite'
  steps:
    - run: |
        echo "Stub: implement Epic Games Store upload here"
        echo "${{ inputs.epic_credentials }}"
        echo "${{ inputs.build_path }}"
      shell: bash